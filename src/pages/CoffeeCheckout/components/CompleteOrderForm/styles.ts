import styled from 'styled-components'
import { SectionDefaultStyle } from '../../styles'

export const CompleteOrderFormContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1.2rem;
  width: 64rem;
`
export const FormContainer = styled(SectionDefaultStyle)`
  display: flex;
  flex-direction: column;
  gap: 3.2rem;
`
